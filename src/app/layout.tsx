import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "UAEPCS | IT Networking, Hardware & Export Solutions",
    template: "%s | UAEPCS",
  },
  description:
    "UAEPCS is a Dubai-based IT retail, wholesale and export solutions provider, established in 1997, specializing in networking equipment, hardware, consumables and corporate IT infrastructure, exporting to over 77 countries.",
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-white text-slate-900">{children}</body>
    </html>
  );
}
